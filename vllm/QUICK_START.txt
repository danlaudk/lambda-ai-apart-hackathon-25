╔══════════════════════════════════════════════════════════════════════════════╗
║                    vLLM REMOTE ACCESS - QUICK START                          ║
╚══════════════════════════════════════════════════════════════════════════════╝

SERVER INFO:
  Public IP:  192.222.53.238
  SSH Access: ssh ubuntu@192.222.53.238
  vLLM Port:  8000

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 STEP-BY-STEP SETUP (5 MINUTES)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1: ON SERVER - Start vLLM
───────────────────────────────────────────────────────────────────────────────

  cd /lambda/nfs/newinstance/vllm
  ./start_vllm_server.sh

  Wait for: "Uvicorn running on http://0.0.0.0:8000"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 2: ON LOCAL COMPUTER - Create SSH Tunnel
───────────────────────────────────────────────────────────────────────────────

  ssh -L 8000:localhost:8000 ubuntu@192.222.53.238

  Keep this terminal open!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 3: ON LOCAL COMPUTER - Test Connection
───────────────────────────────────────────────────────────────────────────────

  Open a NEW terminal and run:

  curl http://localhost:8000/v1/models

  You should see a list of available models!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 4: USE FROM PYTHON
───────────────────────────────────────────────────────────────────────────────

  # Install OpenAI library
  pip install openai

  # Python code:
  from openai import OpenAI

  client = OpenAI(
      base_url="http://localhost:8000/v1",
      api_key="dummy"
  )

  response = client.chat.completions.create(
      model="Qwen/Qwen2.5-14B-Instruct",
      messages=[{"role": "user", "content": "Hello!"}]
  )

  print(response.choices[0].message.content)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📁 FILES CREATED FOR YOU:
───────────────────────────────────────────────────────────────────────────────

  REMOTE_ACCESS_GUIDE.md    - Complete setup guide with all options
  local_client_example.py    - Python examples you can run locally
  create_ssh_tunnel.sh       - Helper script for SSH tunnel
  QUICK_START.txt            - This file!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 TIPS:
───────────────────────────────────────────────────────────────────────────────

  ✅ SSH tunnel is SECURE (encrypted)
  ✅ No firewall changes needed
  ✅ Works from anywhere
  ✅ Can access from multiple local apps simultaneously

  ⚠️  Keep SSH tunnel terminal open while using vLLM
  ⚠️  If connection drops, just restart the SSH tunnel

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 TROUBLESHOOTING:
───────────────────────────────────────────────────────────────────────────────

  Problem: "Connection refused"
  Solution: Check vLLM is running on server (Step 1)

  Problem: "Port already in use"
  Solution: Use different port: ssh -L 8001:localhost:8000 ubuntu@...
            Then use http://localhost:8001 instead

  Problem: SSH asks for password repeatedly
  Solution: Set up SSH key authentication

  Problem: Model not found
  Solution: Wait for model downloads to complete
            Check: ls -lh ~/.cache/huggingface/hub/

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 NEXT STEPS:
───────────────────────────────────────────────────────────────────────────────

  1. Download local_client_example.py to your computer
  2. Run: python local_client_example.py
  3. Try the interactive examples!

  For production setup with HTTPS, see REMOTE_ACCESS_GUIDE.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎉 YOU'RE ALL SET!

Access your H100 GPU with state-of-the-art LLMs from anywhere! 🚀

╚══════════════════════════════════════════════════════════════════════════════╝

